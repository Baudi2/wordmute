"""WordMute — mute unwanted words in video/audio, fully on-device.

__version__ is the single source of truth: the build script injects it
into the installer, and the self-update check compares it against the
latest GitHub release tag."""

__version__ = "0.4.0"
